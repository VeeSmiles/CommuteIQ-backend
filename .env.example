# Copy to .env for local dev. Without these set, reports are stored in
# memory (reset on restart) so the API still works before Supabase is wired up.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-key
FRONTEND_URL=https://commuteiq.vercel.app
